
To create a new version of merkely/change:latest and push it to the dockerhub registry:

1)
$ git commit ...
$ git push

2) Create a tag with 'release-' as a prefix. Eg
$ git tag release-2021-04-28-01
$ git push origin !$


